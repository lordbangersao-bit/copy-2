# Target da aplicação: web | pwa | desktop
VITE_TARGET=web

# URL pública base usada em verificação de documentos e QR codes.
# Em branco → usa a origem actual do browser (comportamento actual).
VITE_PUBLIC_APP_URL=
